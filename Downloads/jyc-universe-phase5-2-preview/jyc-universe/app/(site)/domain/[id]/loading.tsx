import TravelLoading from "@/components/TravelLoading";

/** Worlds pull in a city layout and every club in them. */
export default function Loading() {
  return <TravelLoading label="Approaching the world" />;
}
