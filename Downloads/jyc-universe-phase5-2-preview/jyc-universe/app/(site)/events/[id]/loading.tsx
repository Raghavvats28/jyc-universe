import TravelLoading from "@/components/TravelLoading";

/** The event portal grows from the clicked orb; this is what it grows over while the page renders. */
export default function Loading() {
  return <TravelLoading label="Opening the portal" />;
}
