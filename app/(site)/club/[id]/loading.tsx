import TravelLoading from "@/components/TravelLoading";

/** Club spaces load six floors of content; this covers the gap after the building zoom. */
export default function Loading() {
  return <TravelLoading label="Entering the club" />;
}
