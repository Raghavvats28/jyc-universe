import TravelLoading from "@/components/TravelLoading";

/** The archive joins every club's gallery before the wall can be laid out. */
export default function Loading() {
  return <TravelLoading label="Opening the archive" />;
}
